
# 1.本地安装docker

使用wsl，在本地运行

```bash
sudo apt install docker.io
```

验证运行

```bash
docker version
```

不报错就是安装好了

# 2.测试

测试docker是否工作，在本地运行

```bash
sudo docker run hello-world // 此处不sudo没权限
```

如果你看到如下内容

```

```